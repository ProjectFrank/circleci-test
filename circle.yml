machine:
  node:
    version: 0.12.6

dependencies:
  pre:
    - rm -rf node_modules/node-sass
    - cp nonexistent ~; echo "prevent failure"
  post:
    - echo "post"
  cache_directories:
    - ~/circleci-test/node_modules
    - "package.json"

test:
  override:
    - echo "NO TEST"