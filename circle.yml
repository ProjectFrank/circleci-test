machine:
  node:
    version: 0.12.0

dependencies:
  pre:
    - rm -rf node_modules/node-sass
    - sudo mkdir /home/dsafjkldsjf
  cache_directories:
    - ~/circleci-test/node_modules
    - /home/dsafjkldsjf

test:
  override:
    - echo "NO TEST"