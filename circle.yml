machine:
  node:
    version: 0.12.4
  services:
    - rabbitmq-server

dependencies:
  pre:
    - npm install -g gulp
    - mkdir test_dir
    - cd test_dir && touch cd_file
    - mkdir pwd_dir
    - touch pwd_file:
        pwd:
          pwd_dir
    - npm install -g bower
    - npm install -g karma
  override:
    - bower install jquery
    - echo "NO DEPS"
  cache_directories:
    - ~/circleci-test/node_modules