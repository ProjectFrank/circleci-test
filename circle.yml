machine:
  node:
    version: 0.12.4
  services:
    - rabbitmq-server

dependencies:
  pre:
    - npm install -g gulp
    - npm install -g bower
    - npm install -g karma
  override:
    - echo "NO DEPS"
  cache_directories:
    - ~/circle-test/node_modules

test:
  override:
    - npm run thing