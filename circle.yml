test:
  override:
    - echo "no tests"
      
deployment:
  allthethings:
    tag: /.*/
    commands:
      - echo "deployment"

compile:
  override: 
    - echo stuff

teardown:
  pre:
    - echo "stuff"