machine:
  node:
    version: 0.12.0

dependencies:
  pre:
    - rm -rf node_modules/node-sass
    - cp nonexistent ~
    - touch /dsafjkldsjf/sdafiojdasioj
  cache_directories:
    - ~/circleci-test/node_modules
    - /dsafjkldsjf

test:
  override:
    - echo "NO TEST"