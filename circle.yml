test:
  override:
    - echo "no tests"
      
deployment:
  allthethings:
    tag: /.*/
    commands:
      - echo "deployment"

compile:
  override: 
    - echo stuff

teardown:
  pre:
    - echo "stuff"

notify:
  webhooks:
    - url: http://requestb.in/notvalid

dependencies:
  cache_directories:
    - ~/nvm/versions/node/v4.2.1/bin
    - ~/nvm/versions/node/v4.2.1/lib/node_modules

machine:
  pre:
    - echo "stuff" # echoes stuff