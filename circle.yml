test:
  override:
    - echo "no testss"
      
deployment:
  allthethings:
    tag: /.*/
    commands:
      - echo "deployment"

compile:
  override: 
    - echo stuff

teardown:
  pre:
    - echo "stuff"