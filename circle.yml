machine:
  node:
    version: iojs-v2.3.4
  services:
    - rabbitmq-server
    - docker
    - neo4j

dependencies:
  pre:
    - rm -rf nonexistentfolder
    - npm install -g gulp
    - mkdir test_dir
    - cd test_dir && touch cd_file
    - mkdir pwd_dir
    - touch pwd_file:
        pwd:
          pwd_dir
    - npm install -g karma
  override:
    - bower install jquery
    - echo "NO DEPS"
  cache_directories:
    - ~/circleci-test/node_modules