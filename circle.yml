machine:
#  node:
#    version: 0.12.6
#  python:
#    version: 2.7.6
  post:
    - rvm use 2.2.1

dependencies:
  pre:
    - rm -rf node_modules/node-sass
    - cp nonexistent ~; echo "prevent failure"
    - wget -O boot "https://github.com/boot-clj/boot/releases/download/2.0.0/boot.sh"
    - nvm install 0.12 && nvm alias default 0.12
#  post:
#    - echo "post"
  cache_directories:
    - ~/circleci-test/node_modules
    - "boot"

test:
  override:
    - nvm use default
    - node -v
    - echo "NO TEST"

notify:
  webhooks:
    - url: https://hooks.slack.com/services/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