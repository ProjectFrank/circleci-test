test:
  override:
    - echo "no tests"
      
deployment:
  allthethings:
    branch: /.*/
    commands:
      - ::wait-for-deploy-lock
      - echo "deployment"

compile:
  override: 
    - echo stuff

teardown:
  pre:
    - echo "stuff"

notify:
  webhooks:
    - url: http://requestb.in/notvalid

machine:
  environment:
    testvar: $HOME
  php:
    version: 7.0.0RC7
  node:
    version: 4.2.0