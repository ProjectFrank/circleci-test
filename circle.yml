machine:
#  node:
#    version: 0.12.6
  python:
    version: 2.7.6
  post:
    - rvm info 2.2.1
    - rvm list
    - rvm use 2.2.1
    - ls -la $(which ruby)

dependencies:
  cache_directories:
    - ~/.rvm/gems/ruby-2.2.1
    - ~/.rvm/gems/ruby-2.2.1@global

test:
  override:
    - echo "NO TEST"

notify:
  webhooks:
    - url: https://hooks.slack.com/services/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