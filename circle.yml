machine:
  node:
    version: 0.12.4
  services:
    - rabbitmq

dependencies:
  pre:
    - npm install -g gulp
    - npm install -g bower
    - npm install -g karma
  override:
    - echo "NO DEPS"

test:
  override:
    - npm run thing